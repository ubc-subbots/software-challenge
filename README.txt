HOW TO RUN:

open a terminal and change to root directory, then run the commands:
source /opt/ros/foxy/setup.bash
ros2 run turtlesim turtlesim_node

open a second terminal and change to root directory, then run the commands:
source /opt/ros/foxy/setup.bash
. install/setup.bash
colon build
ros2 run challenge solution
